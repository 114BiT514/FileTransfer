# Qt 文件传输工具 —— 开发步骤与实现说明

> 课程作业：基于 Qt 6 + QTcpSocket / QTcpServer 的 TCP 文件传输工具。
> 本文档记录完整的开发步骤、关键技术点解析、踩坑记录与测试方法，
> 与源码中的中文注释一一对应，可按步骤对照阅读。

---

## 目录

1. [项目概览与架构](#1-项目概览与架构)
2. [开发环境准备](#2-开发环境准备)
3. [步骤 1：创建工程与 .pro 文件](#3-步骤-1创建工程与-pro-文件)
4. [步骤 2：定义自定义传输协议 protocol.h](#4-步骤-2定义自定义传输协议-protocolh)
5. [步骤 3：实现服务端 FileServer（分包组包核心）](#5-步骤-3实现服务端-fileserver分包组包核心)
6. [步骤 4：实现客户端 FileClient（流水线发送核心）](#6-步骤-4实现客户端-fileclient流水线发送核心)
7. [步骤 5：设计主界面 mainwindow.ui](#7-步骤-5设计主界面-mainwindowui)
8. [步骤 6：设置对话框与跨窗口信号槽](#8-步骤-6设置对话框与跨窗口信号槽)
9. [步骤 7：拖拽接收区 DropArea（虚函数重写）](#9-步骤-7拖拽接收区-droparea虚函数重写)
10. [步骤 8：主窗口整合 MainWindow](#10-步骤-8主窗口整合-mainwindow)
11. [步骤 9：资源系统（.qrc）与 QSS 美化](#11-步骤-9资源系统qrc与-qss-美化)
12. [步骤 10：编译、运行与联调测试](#12-步骤-10编译运行与联调测试)
13. [关键技术点深入解析](#13-关键技术点深入解析)
14. [踩坑记录（真实调试过程）](#14-踩坑记录真实调试过程)
15. [作业要求对照表](#15-作业要求对照表)
16. [提交前检查清单与可拓展方向](#16-提交前检查清单与可拓展方向)

---

## 1. 项目概览与架构

### 1.1 设计思路

客户端与服务端**置于同一工程、同一程序**：同一个程序既可以点击"启动监听"充当
**服务端**，也可以点击"连接服务器"充当**客户端**。本地测试时启动两个实例即可互传，
联调最方便，也符合作业"可置于同一项目"的要求。

严格按 **View / Model 分离**：

```
┌───────────────────────────  View 层（界面显示） ───────────────────────────┐
│  MainWindow（主窗口）          SettingsDialog（设置对话框）    DropArea（拖拽区） │
│  日志/进度条/按钮/菜单/状态栏    IP、端口、保存目录              拖入文件高亮/放下  │
└───────────────△──────────────────────△───────────────────────△────────────┘
                │ 信号(log/progress/state)│ settingsApplied(AppSettings)│ fileDropped(path)
┌───────────────┴──────────────────────┴───────────────────────┴────────────┐
│                          Model 层（网络与传输逻辑）                          │
│   FileServer（接收端）                        FileClient（发送端）             │
│   QTcpServer 监听 / 接受连接                  QTcpSocket 连接服务器            │
│   协议状态机：分包 + 组包                      协议头组包 + bytesWritten 流水线   │
└───────────────────────────────┬───────────────────────────────────────────┘
                                │ TCP 字节流（自定义协议：类型+名长+文件名+大小+数据）
                        ┌───────┴───────┐
                        │  对端 FileTransfer  │   （两个实例互为对端）
                        └───────────────┘
```

* View 层**不含任何** socket 读写代码，只连接信号刷新界面；
* Model 层**不含任何** UI 代码，只通过信号上报日志、进度、状态；
* `protocol.h` 是双方共同遵守的报文格式定义。

### 1.2 目录结构

```
FileTransfer/
├── FileTransfer.pro          # 工程文件（QT += network）
├── main.cpp                  # 程序入口：加载 QSS 样式表、应用图标
├── protocol.h                # 【协议】报文格式常量 + 大小格式化工具
├── fileserver.h / .cpp       # 【Model】服务端：监听、接受连接、分包组包、保存文件
├── fileclient.h / .cpp       # 【Model】客户端：连接、协议头组包、流水线发送
├── mainwindow.h / .cpp       # 【View】主窗口：信号槽中枢、日志、进度、状态
├── mainwindow.ui             # 主界面（Qt Designer，纯布局管理器排版）
├── settingsdialog.h / .cpp   # 【View】连接设置对话框（跨窗口信号槽）
├── settingsdialog.ui         # 对话框界面
├── droparea.h / .cpp         # 拖拽接收区（dragEnterEvent 等 4 个虚函数重写）
├── resources/
│   ├── resources.qrc         # 资源系统：图标 + QSS 打包进可执行文件
│   ├── style.qss             # 全局样式表（按钮悬停、进度条、拖拽高亮等）
│   └── icons/*.png           # 图标（由 tools/gen_icons 生成，QPainter 手绘）
├── tools/                    # 可选：图标生成工具（不参与主程序编译）
└── test/                     # 可选：协议自测程序（粘包/分包/MD5 校验）
```

---

## 2. 开发环境准备

| 项目 | 要求 |
|------|------|
| 语言 | C++17 |
| Qt | Qt 6.x（本项目在 Qt 6.2 下编译通过；Qt 5.15 亦可少量调整） |
| IDE | Qt Creator |
| 模块 | `QtCore`、`QtGui`、`QtWidgets`、**`QtNetwork`** |

1. 安装 Qt 在线安装器，勾选 **Qt 6.x**（Desktop 套件自带 Qt Network、Qt Creator）。
2. Windows 下选 MinGW 或 MSVC 套件均可（.pro 已为 MSVC 加了 `/utf-8`，中文注释不乱码）。
3. 验证：Qt Creator → 欢迎 → 新建工程能看到 "Application (Qt) → Qt Widgets Application"。
4. 遇到 API 疑问随手查 **Qt Assistant**（`assistant`）或在线文档，例如搜索
   `QTcpSocket::bytesWritten`、`QDragEnterEvent`，这是作业"培养工程查阅能力"的日常动作。

---

## 3. 步骤 1：创建工程与 .pro 文件

**目标**：建空工程，声明 network 模块。

1. Qt Creator → New Project → Qt Widgets Application，工程名 `FileTransfer`，
   构建系统选 **qmake**（生成 .pro）。
2. 修改 `FileTransfer.pro`，关键三处：

```pro
QT += core gui network          # ← network 模块提供 QTcpSocket / QTcpServer
greaterThan(QT_MAJOR_VERSION, 4): QT += widgets
CONFIG += c++17

msvc: QMAKE_CXXFLAGS += /utf-8  # MSVC 按 UTF-8 读源码，中文注释不乱码

SOURCES += main.cpp mainwindow.cpp settingsdialog.cpp droparea.cpp fileclient.cpp fileserver.cpp
HEADERS += mainwindow.h settingsdialog.h droparea.h fileclient.h fileserver.h protocol.h
FORMS   += mainwindow.ui settingsdialog.ui
RESOURCES += resources/resources.qrc   # 资源系统
```

**自查**：加一行 `#include <QTcpSocket>` 能编译通过即说明 network 模块已生效。

---

## 4. 步骤 2：定义自定义传输协议 protocol.h

**目标**：解决 TCP 粘包 / 拆包。这是本作业**最核心**的考点。

### 4.1 为什么会有粘包 / 拆包？

TCP 是**字节流**协议，只保证"发出去的字节按顺序完整到达"，**不保留消息边界**：

```
发送端                          接收端 readAll() 实际看到的
─────────────────────────────────────────────────────────
write(报文A)  ────┐
write(报文B)  ────┼──→  可能一次收到 "AB"        （粘包：两包并一包）
                  └──→  可能先收到 "A 的一半"     （拆包：一包分两次）
                        再收到 "A 的后半 + B"
```

因此必须**自定义协议头**，告诉接收方"一段完整数据有多长"，接收方据此切割字节流。

### 4.2 报文格式（protocol.h）

```
+-------------+--------------+--------------+-------------+-------------+
| 消息类型 4B  | 文件名长度 4B | 文件名 N B   | 数据大小 8B  | 文件数据 M B |
+-------------+--------------+--------------+-------------+-------------+
  type(MT_*)    nameLen        name(UTF-8)    fileSize      payload
  └────────── 固定头 16 字节 ──────────────┘
```

* `MT_TEXT=1`：文本消息，`nameLen=0`（用于测试连通性）；
* `MT_FILE=2`：文件消息，携带文件名，payload 是**任意二进制**——
  .txt 与 .png/.jpg 在本协议里没有区别，都是字节，天然支持"文本文件+图片"两类需求；
* 字节序统一**大端**（QDataStream 默认，两端一致即可）。

对应代码见 `protocol.h`，只有常量与一个 `formatFileSize()` 辅助函数，双方共同 include。

---

## 5. 步骤 3：实现服务端 FileServer（分包组包核心）

**目标**：监听端口 → 接受连接 → 按"协议状态机"组包 → 落盘保存。

### 5.1 监听与接受连接（fileserver.cpp 构造函数 / onNewConnection）

```cpp
m_server = new QTcpServer(this);
// 【信号与槽】新客户端接入 -> onNewConnection 里 nextPendingConnection() 接受
connect(m_server, &QTcpServer::newConnection, this, &FileServer::onNewConnection);

void FileServer::onNewConnection() {
    while (QTcpSocket *sock = m_server->nextPendingConnection()) { // 循环取完所有 pending
        ClientCtx *ctx = new ClientCtx;          // 每个客户端一个"接收上下文"
        m_clients.insert(sock, ctx);
        connect(sock, &QTcpSocket::readyRead,  this, &FileServer::onReadyRead);
        connect(sock, &QTcpSocket::disconnected, this, &FileServer::onDisconnected);
        connect(sock, &QTcpSocket::disconnected, sock, &QObject::deleteLater);
    }
}
```

> 备注：作业允许 `incomingConnection` / `newConnection` 二选一。等价的另一种写法是
> 重写 `QTcpServer::incomingConnection(qintptr socketDescriptor)`，在其中
> `new QTcpSocket` 并 `setSocketDescriptor()`，本项目采用更常用的 `newConnection` 信号。

### 5.2 分包组包：三状态协议状态机

每个客户端连接对应一个 `ClientCtx`（缓冲区 + 状态 + 当前报文信息）。`readyRead`
到达的数据**先追加进缓冲区**，再循环推进状态机：

```
            缓冲 ≥ 16B                缓冲 ≥ nameLen             数据收满 fileSize
┌──────────┐ ─────────→ ┌──────────┐ ─────────→ ┌──────────┐ ─────────→ 发信号/落盘，
│WaitHeader│            │ WaitName │            │ WaitData │            回到 WaitHeader
└──────────┘ ←───────── └──────────┘ ←───────── └──────────┘
        （数据不够就 return false，等下一次 readyRead 再续）
```

```cpp
void FileServer::onReadyRead() {
    ClientCtx *ctx = m_clients.value(sock);
    ctx->buffer += sock->readAll();            // ① 分次到达的数据在这里拼接
    bool progressed = true;
    while (progressed) {                       // ② 够一段就解析一段（解决粘包）
        switch (ctx->state) {
        case WaitHeader: progressed = parseHeader(ctx); break;  // 读 16B 固定头
        case WaitName:   progressed = parseName(ctx);   break;  // 读文件名并建 .part 文件
        case WaitData:   processData(ctx);              break;  // 到多少数据写多少
        }
    }
}
```

三个要点：

1. **parseHeader / parseName 数据不足时直接返回 false**——剩下的字节留在缓冲区，
   等下一次 `readyRead` 接上（这就是"分次到达的数据拼接"）；
2. **processData 每次只消费 `min(缓冲长度, 还差多少)`**，收满 `fileSize` 才算一段完成；
3. **一段完成后不清空缓冲区**——后面可能已经躺着下一条报文（粘包场景），
   状态机回到 `WaitHeader` 继续从残余字节解析。

### 5.3 可靠落盘细节

* 收到的文件先写 **`文件名.part` 临时文件**，全部收完再改名——收到一半断线不会留下
  "看似完整的坏文件"，同时删除 `.part` 残留；
* **同名文件自动加序号** `name(1).ext`，不覆盖旧文件；
* 保存路径只取 `QFileInfo(fileName).fileName()`，**防对端路径穿越**（如 `..\..\x`）；
* 协议头字段带**合法性上限校验**（名字 ≤4KB、大小 ≤2GB），非法报文直接断开该客户端；
* 进度信号做了**节流**：每 256KB 或收完时才 `emit` 一次，避免高频信号拖慢界面。

---

## 6. 步骤 4：实现客户端 FileClient（流水线发送核心）

**目标**：连接服务器 → 组协议头 → 按"write + bytesWritten 流水线"发数据。

### 6.1 连接管理（全异步）

```cpp
m_socket->connectToHost(host, port);   // 异步发起，立即返回，界面不卡
connect(m_socket, &QTcpSocket::connected,     this, &FileClient::onConnected);
connect(m_socket, &QTcpSocket::disconnected,  this, &FileClient::onDisconnected);
connect(m_socket, &QTcpSocket::errorOccurred, this, &FileClient::onErrorOccurred);
```

连接失败（拒绝/超时）、对端断开、发送中断线，全部走信号给界面提示，程序不崩溃。

### 6.2 为什么不能"一次 write 整个文件"？

`m_socket->write(整个文件)` 会把**几 GB 数据整个拷进 Qt 发送缓冲区**，内存瞬间爆掉。
作业要求的做法（也是本项目的做法）：

```
startTransfer()  ──write(协议头+文件名)──→  pump() 写第 1 块 64KB
     ▲                                          │
     │                                          ▼
finishTransfer() ←── 数据发完且 bytesToWrite()==0        OS 网络缓冲
     ▲                                          │
     │  每当 bytesWritten(n) 通知"又写出 n 字节"  ▼
     └──────────── onBytesWritten() → pump() 写下一块 …… 循环直到文件读完
```

```cpp
void FileClient::pump() {
    if (m_socket->bytesToWrite() >= 64KB * 4)  return; // 水位线：缓冲满就等信号
    读 64KB → m_socket->write(block);                  // 只写一小块
    if (m_queued >= m_fileSize && m_socket->bytesToWrite() == 0)
        finishTransfer(true);                          // 全部写出 → 完成
}
void FileClient::onBytesWritten(qint64 bytes) {        // bytesWritten 驱动流水线
    m_acked += bytes;  emit sendProgress(...);         // 用真实写出字节数更新进度
    pump();                                            // 续写下一块
}
```

效果：**内存占用恒定约 256KB**，发送速度自动适配网络带宽；进度百分比来自
`bytesWritten` 的真实确认，不是估算。

### 6.3 协议头组装

```cpp
QDataStream out(&header, QIODevice::WriteOnly);
out.setVersion(QDataStream::Qt_6_0);       // 两端约定同一版本（大端序）
out << m_type << quint32(name.size()) << quint64(m_fileSize);
header.append(name);                       // 文本消息 name 为空 => nameLen=0
m_socket->write(header);                   // 头较小，一次写出
```

同一时刻只允许一个传输任务（`isBusy()` 保护），这样 `bytesWritten` 的字节计数
一定能归属到当前传输，进度计算不会串台。

---

## 7. 步骤 5：设计主界面 mainwindow.ui

**目标**：全部用**布局管理器**排版，禁止绝对坐标。

| 区域 | 控件 | 说明 |
|------|------|------|
| 工具行 | 5×QPushButton + 弹簧 | 连接设置 / 启动监听 / 连接服务器 / 选择文件发送 / 打开保存目录 / 退出 |
| 拖拽区 | **DropArea**（提升控件） | QFrame 子类，见步骤 7 |
| 进度区 | QLabel + QProgressBar + QLabel | 任务名、百分比、已传字节 |
| 底部左 | QGroupBox（QTextBrowser + QLineEdit + 按钮） | 传输日志 + 文本消息发送 |
| 底部右 | QGroupBox（3×QLabel） | 客户端状态 / 服务端状态 / 保存目录 |
| 其他 | QMenuBar + QStatusBar | 菜单快捷键、状态栏常驻信息 |

操作要点：

1. 顶层 `QVBoxLayout`，底部 `QHBoxLayout`，按钮行 `QHBoxLayout`，全部在 Designer
   里拖拽完成——窗口缩放时控件自适应（.ui 里没有任何 geometry 硬编码）；
2. 日志区/状态区宽度比、纵向占比在代码里用 `setStretch()` 设置（uic 不支持逐项 stretch）；
3. DropArea 通过 **提升控件**（Promote to → `DropArea`，头文件 `droparea.h`）嵌入，
   .ui 中出现 `<customwidget>` 声明，uic 编译期生成对应 `new DropArea(...)`；
4. 菜单动作带快捷键：`Ctrl+O` 选择文件、`Ctrl+Q` 退出、`Ctrl+Shift+C` 连接设置。

**自查**：拖动窗口边缘缩放，各区域按比例伸缩、控件不重叠不出界。

---

## 8. 步骤 6：设置对话框与跨窗口信号槽

**目标**（必做项）：对话框收集参数，**通过自定义信号**传给主窗口。

### 8.1 界面（settingsdialog.ui）

`QFormLayout`：服务器 IP（QComboBox 可编辑）｜端口（QSpinBox 1~65535，默认 8888）｜
接收保存目录（QLineEdit + "浏览..."）。底部 `QDialogButtonBox`（OK/Cancel）。

### 8.2 参数传递：自定义信号

对话框头文件里定义参数结构体与信号：

```cpp
struct AppSettings {
    QString host;          // 服务器 IP
    quint16 port = 8888;
    QString saveDir;       // 接收保存目录
};

signals:
    void settingsApplied(const AppSettings &settings);   // 确定 -> 主窗口
```

点"确定"时校验（IP/目录非空，否则 QMessageBox 提示且不关窗），然后：

```cpp
emit settingsApplied(s);   // 【跨窗口信号槽】参数随信号飞向主窗口
accept();
```

主窗口打开对话框时连接一次：

```cpp
m_settingsDialog = new SettingsDialog(this);
// 【跨窗口通信】对话框 -> 主窗口
connect(m_settingsDialog, &SettingsDialog::settingsApplied,
        this, &MainWindow::onSettingsApplied);
...
m_settingsDialog->setSettings(m_settings);  // 打开前填入当前值
m_settingsDialog->exec();                   // 模态显示
```

`onSettingsApplied` 收到参数后：更新 `m_settings`、保存目录立即生效、刷新界面标签、
并用 `QSettings` **持久化**（下次启动自动恢复上次的 IP/端口/目录）。

### 8.3 事件过滤器（顺带考点）

对话框把自身安装为 IP 输入框与目录输入框的**事件过滤器**：拦截 `FocusIn` 事件让
文字自动全选，方便直接覆盖输入——

```cpp
ui->cbIp->lineEdit()->installEventFilter(this);   // 安装
ui->leSaveDir->installEventFilter(this);

bool SettingsDialog::eventFilter(QObject *watched, QEvent *event) {
    if (event->type() == QEvent::FocusIn)                     // 拦截"获得焦点"
        if (auto *edit = qobject_cast<QLineEdit *>(watched))
            edit->selectAll();
    return QDialog::eventFilter(watched, event);              // 其余事件放行
}
```

---

## 9. 步骤 7：拖拽接收区 DropArea（虚函数重写）

**目标**：文件拖进窗口高亮，松手即发送（客户端）或保存（服务端）。

`DropArea` 继承 `QFrame`，构造函数 `setAcceptDrops(true)`，然后重写 4 个**拖拽虚函数**：

```cpp
void dragEnterEvent(QDragEnterEvent *e) override {   // 拖入窗口边界
    if (e->mimeData()->hasUrls()) {                  // 拖的是文件才接受
        e->acceptProposedAction();
        setDragOver(true);                           // 高亮
    } else e->ignore();
}
void dragMoveEvent(QDragMoveEvent *e) override;      // 区域内移动：持续接受
void dragLeaveEvent(QDragLeaveEvent *e) override;    // 拖离/取消：取消高亮
void dropEvent(QDropEvent *e) override {             // 松开鼠标：交出文件
    for (const QUrl &url : e->mimeData()->urls())
        if (url.isLocalFile()) emit fileDropped(url.toLocalFile());
}
```

主窗口只接一个信号，按角色分流——**对应作业"发送（客户端）或保存（服务端）"**：

```cpp
connect(ui->dropArea, &DropArea::fileDropped, this, &MainWindow::onFileDropped);
// onFileDropped: 已连接 -> m_client->sendFile(path)（发送）
//                未连接 -> 复制到接收目录（保存），同名自动加序号
```

**高亮实现**：切换**动态属性** `dragOver`，配合 QSS 属性选择器变色（见步骤 9）。
注意属性改变后必须 `style()->unpolish(this); style()->polish(this);` 才会立即生效。

---

## 10. 步骤 8：主窗口整合 MainWindow

**目标**：把所有对象用信号槽"接线"，界面只做三件事——显示日志、显示进度、提示错误。

### 10.1 连接清单（mainwindow.cpp 构造函数，源码内有逐条注释）

| 信号源 | 信号 | 主窗口槽 |
|--------|------|----------|
| 按钮/菜单 | clicked / triggered | onToggleListen、onToggleConnect、onOpenFileToSend… |
| DropArea | fileDropped(path) | onFileDropped |
| SettingsDialog | settingsApplied(AppSettings) | onSettingsApplied |
| FileServer | stateChanged / clientConnected / clientDisconnected | 刷新状态标签、在线数 |
| FileServer | recvStarted / recvProgress / recvFinished / recvFailed | 进度条、日志、状态栏提示 |
| FileServer | textReceived(peer, text) | 日志显示文本消息 |
| FileClient | stateChanged / sendStarted / sendProgress / sendFinished / sendFailed | 按钮文字、进度条、日志 |
| FileServer / FileClient | logInfo / logError | appendLog（按级别着色） |

### 10.2 日志区

`appendLog(msg, level)` 给每条记录加时间戳并按级别着色（普通=浅蓝、成功=绿、
警告=黄、错误=红），用 `QTextBrowser::append()` 插入 HTML `<span>`，
文件名经过 `toHtmlEscaped()` 防止特殊字符破坏排版。

### 10.3 错误处理（作业 2.1 最后一条）

| 场景 | 表现 |
|------|------|
| 连接被拒/超时 | logError 红字 + 按钮状态自动恢复（onErrorOccurred） |
| 端口被占用 | QMessageBox 提示 + 日志（startListen 返回 false） |
| 文件打开失败 | logError（发送端/接收端各自捕获 QFile::errorString） |
| 传输中断线 | 发送端 sendFailed / 接收端删除 .part 残留 + recvFailed |
| 非法协议数据 | 服务端断开该客户端并记日志，不影响其他客户端 |
| 监听中直接关窗 | 见"踩坑记录 3"，析构顺序已处理，不崩溃 |

### 10.4 设置持久化

`QSettings`（组织名/应用名在 main.cpp 设置）：`network/host`、`network/port`、
`paths/saveDir`，启动时 `loadSettings()` 恢复，对话框确定后写回。
默认保存目录为系统下载目录下的 `QtFileTransfer/`。

---

## 11. 步骤 9：资源系统（.qrc）与 QSS 美化

### 11.1 资源系统

`resources/resources.qrc` 把 8 个图标和 `style.qss` 编进可执行文件：

```xml
<RCC version="1.0">
  <qresource prefix="/">
    <file>icons/connect.png</file>
    ...
    <file>style.qss</file>
  </qresource>
</RCC>
```

代码中以 `:/` 前缀访问：`QIcon(":/icons/send.png")`、`QFile f(":/style.qss")`（main.cpp
里读出后 `app.setStyleSheet()`）。

> 图标是线框风格 PNG，由 `tools/gen_icons`（QPainter 手绘，96×96 抗锯齿）生成，
> 不依赖 qtsvg 模块，任何 Qt 6 环境都能正确渲染；想改图标可重新运行该工具。

### 11.2 QSS 关键选择器（resources/style.qss）

```css
QPushButton { background:#2f81f7; border:none; border-radius:6px; }
QPushButton:hover { background:#1f6feb; }        /* 悬停变色 */
QPushButton:disabled { background:#b9cfe8; }

QProgressBar { border-radius:9px; text-align:center; }
QProgressBar::chunk { background: qlineargradient(...); }  /* 渐变进度块 */

QTextBrowser#logView { background:#1e2430; }     /* 深色终端风日志区 */

/* 拖拽区两种状态：普通虚线框 / 拖入高亮（配合动态属性） */
DropArea#dropArea { border: 2px dashed #b6c6d9; }
DropArea#dropArea[dragOver="true"] { background:#e8f2ff; border-color:#2f81f7; }
```

---

## 12. 步骤 10：编译、运行与联调测试

### 12.1 编译

**方式 A（Qt Creator）**：打开 `FileTransfer.pro` → 配置 Kit → 左下角锤子构建 → 绿三角运行。

**方式 B（命令行）**：

```bash
cd FileTransfer
mkdir build && cd build
qmake ../FileTransfer.pro        # Windows 下是 qmake.exe（Qt Creator 自带）
make -j8                         # MinGW 为 mingw32-make；MSVC 在命令行用 nmake 或 jom
```

### 12.2 本地双实例联调（推荐流程）

1. 启动**实例 A**：点击 **启动监听**（默认端口 8888）→ 标题栏显示 [监听中 :8888]；
2. 启动**实例 B**：**连接设置** → IP `127.0.0.1`、端口 8888、选好保存目录 → 确定 →
   点击 **连接服务器** → 日志显示"已连接"，A 侧显示"客户端已连接：127.0.0.1:xxxx"；
3. **传文本**：B 底部输入框输入文字回车 → A 日志出现文本消息（快速验证连通性）；
4. **传文件**：
   - B 里点"选择文件发送"或**把文件拖进虚线框**；
   - 双方进度条实时推进，A 完成后状态栏提示保存路径；
5. **验证一致性**（作业要求"内容与原文件一致"）：
   - Windows：`fc /b 原文件 保存的文件`；
   - Linux/macOS：`md5sum 原文件 保存的文件`；
   - 也可发送一张图片，接收方双击打开确认可正常显示；
6. **异常演练**：传输中途直接关掉 A → B 日志出现"发送失败"，程序不崩；
   把 B 连到不存在的端口 → "连接被拒绝"提示；A 重启后再传同名文件 → 自动保存为
   `name(1).ext`。

### 12.3 协议自测程序（可选，test/ 目录）

```bash
cd FileTransfer/test && mkdir build && cd build
qmake ../selftest.pro && make -j8
./filetransfer-selftest
# 预期输出： ... SELFTEST PASS
```

它把服务端套接字读缓冲**故意限制到 16KB**（制造"拆包"），并让客户端**连续发送**
文本 → 3MB 大文件 → 小文件（制造"粘包"），最后对收到的文件做 **MD5 比对**。
`SELFTEST PASS` = 三次内容逐字节一致，协议实现正确。

---

## 13. 关键技术点深入解析

### 13.1 一次真实传输的字节流时间线（粘包+拆包同时发生）

以"发文本(41B) 后立刻发文件 big_test.bin"为例，接收端实际收到：

```
第 1 次 readyRead: [文本头16B][文本41B][文件头16B][文件名12B][数据…9B]
                    └──── 报文1 完整 ────┘└──────── 报文2 的开头被"粘"过来了 ────────┘
状态机：WaitHeader 解析报文1 → WaitData 收满 41B → 报文1 完成
        → 循环继续 → WaitHeader 解析报文2（用缓冲区里剩下的字节）
        → WaitName 读文件名 → WaitData …
第 2..n 次 readyRead: [数据…][数据…]…  每次到多少写多少，直到收满 fileSize
```

若对端网速慢，也可能变成"第 1 次只有 16B 头的前 10B"——无论怎么切，
**状态机+缓冲区**都能正确拼装，这就是自定义协议头的意义。

### 13.2 流水线发送与 TCP 背压

`bytesToWrite()` 是 Qt 尚未写入 OS 的排队量。pump() 以 256KB 为水位线：
低于就再写 64KB；高于就什么都不做，等 `bytesWritten` 把水位"抽下去"。
于是：网络快 → 信号来得勤 → 写得快；网络慢 → 水位长期高 → 自动放缓。
**发送内存占用恒定，速度自动匹配对端**，这正是"事件驱动"的精髓：
代码从不主动等待，一切由信号推动。

### 13.3 信号槽在本项目中的三种用法

1. **控件 → 槽**：`connect(btn, &QPushButton::clicked, this, &MainWindow::...)`；
2. **跨窗口自定义信号**：`SettingsDialog::settingsApplied(AppSettings)` → 主窗口，
   自定义结构体随信号传递；
3. **Model → View 解耦**：FileServer/FileClient 只 `emit`，MainWindow 只 `connect`，
   传输类可以脱离界面单独测试（test/ 自测程序就是这么做的）。

---

## 14. 踩坑记录（真实调试过程）

以下三个 bug 都是本项目实际遇到并修复的，调试方法值得记录：

1. **`QFile` 忘记 include**：`fileserver.h` / `fileclient.h` 用了 `QFile*` 成员却只
   include 了网络头文件，报"`QFile` does not name a type"，并连带出一串
   "no member named 'file'"。教训：头文件里用到的类型都要自己 include 全。

2. **finishTransfer 先发信号后复位 —— 重入 bug（最难查）**：
   症状：自测程序发文本正常、紧接着的 3MB 文件"凭空消失"，服务端收不到任何数据。
   用逐函数 fprintf 跟踪后定位：`sendFinished` 的接收方在槽里**立即发起下一次传输**，
   而此时外层 `finishTransfer` 还没执行完——末尾的"复位状态"代码把新传输刚写入的
   `m_queued / m_fileSize` 全部清零，传输悄悄失踪。
   修复：**先复位状态、再 emit 信号**。教训：信号槽是同步直连（同线程），
   emit 一词等价于"立刻调用所有接收者的槽"，必须考虑重入。

3. **监听中关闭窗口必崩 —— 析构顺序 bug**：
   gdb 栈回溯显示崩溃在 `MainWindow::~MainWindow` → 子对象析构 `FileServer::~FileServer`
   → `stopListen()` → `emit logInfo` → `appendLog` 访问**已 delete 的日志控件**。
   因为 `FileServer` 是 MainWindow 的子对象，在 `delete ui` **之后**才被析构，
   而它的析构还会发信号。修复：`~MainWindow` 里先 `stopListen()` /
   `disconnectFromServer()` 并 `disconnect(this)` 切断信号，再 `delete ui`。

---

## 15. 作业要求对照表

| 作业要求 | 实现位置 |
|----------|----------|
| 服务端监听 `QTcpServer::listen` / 接受连接 | `fileserver.cpp` startListen / onNewConnection |
| 客户端连接 `QTcpSocket::connectToHost` | `fileclient.cpp` connectToServer |
| 自定义协议头（类型+名长+文件名+大小+数据） | `protocol.h` + fileclient.cpp startTransfer |
| 分包/粘包处理（状态机组包） | `fileserver.cpp` onReadyRead / parseHeader / parseName / processData |
| 文本文件、图片传输（任意二进制） | MT_FILE 报文，收发均为原始字节，MD5 自测通过 |
| 接收方同名保存 | fileserver.cpp uniqueSavePath（同名自动加序号） |
| 大文件不爆内存（write+bytesWritten 流水线） | fileclient.cpp pump / onBytesWritten（64KB 块 + 256KB 水位线） |
| 实时日志（连接/进度/文件名/大小） | mainwindow.cpp appendLog + 各信号槽 |
| QProgressBar 进度条 | mainwindow.ui progressBar + onSend/onRecvProgress |
| 错误处理不崩溃（QMessageBox/状态栏/日志） | onErrorOccurred、startListen、finishTransfer、recvFailed 等 |
| 拖拽发送/保存（虚函数重写） | droparea.cpp dragEnterEvent 等 4 个重写 + mainwindow onFileDropped |
| 拖入高亮反馈 | DropArea 动态属性 dragOver + style.qss 属性选择器 |
| 布局管理器排版、无绝对坐标 | mainwindow.ui / settingsdialog.ui 全部 Layout |
| QSS 美化（悬停/圆角/高亮） | resources/style.qss |
| 资源系统 .qrc 引入图标 | resources/resources.qrc + iconRes(":/icons/…") |
| 设置对话框 + 跨窗口信号槽（必做） | settingsdialog.settingsApplied → MainWindow::onSettingsApplied |
| 架构分离（FileSender/FileReceiver） | FileClient（发送器）/ FileServer（接收器），View 零网络代码 |
| 事件过滤器 | settingsdialog.cpp eventFilter（焦点全选） |
| .pro 添加 network 模块 | FileTransfer.pro `QT += network` |
| 中文注释（信号槽/读写/分包组包/事件处理） | 全部源码，重点函数均有块注释 |

---

## 16. 提交前检查清单与可拓展方向

### 检查清单

- [ ] 换一台机器（或 Clean 重新 qmake）能直接编译运行；
- [ ] 提交包内包含：`.pro`、全部 `.h/.cpp`、`.ui`、`.qrc`、`style.qss`、图标；
- [ ] **删除 build 目录**（当前仓库已清理，只保留源码）；
- [ ] 双实例互传文本文件、图片各一次，接收端打开验证内容/图像正常；
- [ ] 演示拖拽发送、拖入高亮、进度条、日志、同名自动改名；
- [ ] 演示异常场景：错误 IP、占用端口、传输中断线，均只提示不崩溃。

### 可拓展方向（报告"不足与展望"素材）

* 多文件拖拽排队队列（当前一次一个，其余提示稍后再试）；
* 文件 MD5 校验报文（收尾校验完整性）、断点续传（记录 .part 偏移）;
* 多客户端并发文件互传 UI（服务端已支持多连接，可加在线列表选择对端）;
* 传输速率/剩余时间估算；
* 加密传输（QSslSocket 替换，接口几乎一致）。
